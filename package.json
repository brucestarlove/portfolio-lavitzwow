{
  "name": "lavitzio",
  "version": "1.0.0",
  "description": "Lavitzio site",
  "main": "gulpfile.js",
  "scripts": {
    "build": "gulp build && cp -r assets dist/ && cp -r css dist/ && cp -r js dist/ && cp *.html dist/",
    "dev": "gulp watch",
    "vercel-build": "gulp build && cp -r assets dist/ && cp -r css dist/ && cp -r js dist/ && cp *.html dist/",
    "netlify-build": "gulp build && cp -r assets dist/ && cp -r css dist/ && cp -r js dist/ && cp *.html dist/"
  },
  "dependencies": {
    "bootstrap": "^4.3.1"
  },
  "devDependencies": {
    "gulp": "^4.0.2",
    "gulp-babel": "^8.0.0",
    "gulp-cssnano": "^2.1.3",
    "gulp-rename": "^1.4.0",
    "gulp-sass": "^4.0.2",
    "js-yaml": "^3.13.1"
  },
  "author": "",
  "license": "ISC"
}
